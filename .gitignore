# Node modules
node_modules/

# Expo-related folders
.expo/

# Build artifacts
dist/
npm-debug.*
web-build/

# Keys and certificates (don't push sensitive keys)
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# macOS system files
.DS_Store

# Expo-cli generated files
expo-env.d.ts

# Admin and utils folder (don't push admin-only code)
utils/
admin/

# Additional files to exclude (optional)
*.orig.*
